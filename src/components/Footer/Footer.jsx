import React from 'react';
import './Footer.module.css';

const Footer = (props) => {
    return(
        <footer>
            <h3>Отличная отделка</h3>
        </footer>
    )
};

export default Footer;